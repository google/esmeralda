# infrastructure/live/prd/env.yaml
locals {
  environment         = "prd"
  project_prefix      = "esmeralda-prd"
  region              = "us-central1"

  # 📦 REMOTE STATE CONFIGURATION:
  state_project       = ""
  state_bucket        = ""

  # 🔌 BYO INFRA TOGGLES: Greenfield PRD creates isolated projects, reusing only shared CI/CD
  byo_net_host_project   = false
  byo_gateway_project    = false
  byo_governance_project = false
  byo_cicd_project       = true
  byo_networking         = false
  byo_apigee             = false

  existing_net_host_project   = ""
  existing_governance_project = ""
  existing_gateway_project    = ""
  existing_cicd_project       = "esmeralda-cicd-artifacts-3a3d"
  existing_vpc_id             = ""
  existing_subnet_id          = ""

  gateway_product     = "kong"
  database_product    = "cloud-sql"

  billing_account     = "015DBB-3B8B10-665EB3"
  org_id              = "828556469249"
  folder_id           = ""

  # 📊 OBSERVABILITY & MONITORING CONFIGURATION:
  monitoring_config = {
    notification_channels = []
    token_anomaly_threshold_5m = 2000000     # 2M tokens in 5 mins
    p95_latency_threshold_ms   = 5000        # 5s P95
    error_rate_threshold_pct   = 0.5         # 0.5% HTTP 5xx
    enable_token_circuit_breaker   = true
    enable_tco_volume_alerts       = true
    enable_reasoning_engine_alerts = true
  }
}

