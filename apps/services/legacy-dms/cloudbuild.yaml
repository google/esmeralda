# apps/services/legacy-dms/cloudbuild.yaml
# Cloud Build pipeline for Legacy DMS Microservice container build & Agent Registry registration across spoke projects

steps:
  # Step 1: Build Docker Container
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '${_REGION}-docker.pkg.dev/${_CICD_PROJECT_ID}/esmeralda-containers/legacy-dms:${_TAG}', '.']

  # Step 2: Push Docker Image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '${_REGION}-docker.pkg.dev/${_CICD_PROJECT_ID}/esmeralda-containers/legacy-dms:${_TAG}']

  # Step 3: Register / Sync MCP service in Central Governance Agent Registry
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -e
        if [ -n "${_GOVERNANCE_PROJECT_ID}" ]; then
          GOV_PROJECT="${_GOVERNANCE_PROJECT_ID}"
        else
          GOV_PROJECT=$$(gcloud secrets versions access latest --secret="secret-esmeralda-governance-id-${_ENV}" --project="${_CICD_PROJECT_ID}")
        fi
        echo "Syncing legacy-dms in Central Governance Project $$GOV_PROJECT..."

        gcloud agent-registry services create legacy-dms \
          --project="$$GOV_PROJECT" \
          --location="${_REGION}" \
          --display-name="legacy-dms" \
          --description="FastMCP service providing legacy document management and retrieval tools" \
          --interfaces="url=http://legacy-dms.esmeralda.internal/mcp,protocolBinding=jsonrpc" \
          --mcp-server-spec-type=tool-spec \
          --mcp-server-spec-content=tools.json || \
        gcloud agent-registry services update legacy-dms \
          --project="$$GOV_PROJECT" \
          --location="${_REGION}" \
          --display-name="legacy-dms" \
          --description="FastMCP service providing legacy document management and retrieval tools" \
          --interfaces="url=http://legacy-dms.esmeralda.internal/mcp,protocolBinding=jsonrpc" \
          --mcp-server-spec-type=tool-spec \
          --mcp-server-spec-content=tools.json

substitutions:
  _REGION: 'us-central1'
  _CICD_PROJECT_ID: ''
  _GOVERNANCE_PROJECT_ID: ''
  _TAG: 'latest'
  _ENV: 'dev'
  _PROJECT_PREFIX: 'esm-dev'

options:
  logging: CLOUD_LOGGING_ONLY
  substitution_option: 'ALLOW_LOOSE'
